%% Quorum queues publish their metrics (messages, consumers) on their own tick,
%% default 5 s. 1 s makes queue depths in the demo UI react almost immediately.
[
  {rabbit, [{quorum_tick_interval, 1000}]}
].
